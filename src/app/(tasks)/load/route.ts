
import { migrateArticles } from '@/db/migrationTasks/articles';
import { NextResponse } from 'next/server';

export async function GET(){

    // const results = await migrateArticles();

    // return NextResponse.json(results)

}